import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  header: {
    alignItems: 'center',
    alignSelf: 'stretch',
    borderColor: '#bbbbbb',
    borderTopWidth: 1,
    height: 60,
    justifyContent: 'center'
  },
  title: {
    color: 'red',
    fontSize: 20
  }
});
